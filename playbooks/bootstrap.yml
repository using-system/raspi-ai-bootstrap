---
- name: Bootstrap Raspberry Pi 5 as AI node
  hosts: all
  become: true

  roles:
    - role: base
      tags: base
    - role: docker
      tags: docker
    - role: claude-code
      tags: claude-code
